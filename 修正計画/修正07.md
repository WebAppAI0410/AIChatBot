## 7. ノートモジュール（改訂実装計画）

### 7.1 機能概要

- **ノート管理**: フォルダ階層構造でのノート整理、タグ付け
- **エディタ**: WebViewベースWYSIWYG（React Native WebView + TipTap）
- **AIアシスト**: ノート全体または部分的な編集支援、質問応答
- **タグ管理**: タグの追加・編集・削除、複数ノートへの一括適用
- **ストレージ**: SQLiteベースのローカルストレージ（オフライン対応）

### 7.2 プラン別AIモデルとクォータ

| プラン | AIモデル | 制限 | トークン上限 |
|--------|---------|------|------------|
| Free | openai/gpt-4o-mini | 5回/日 | 1,000トークン/回 |
| Lite | openai/gpt-4o | 20回/日 | 2,000トークン/回 |
| Premium | anthropic/claude-3-sonnet-20240229 | 50回/日 | 4,000トークン/回 |

### 7.3 データベース設計（SQLite）

```sql
-- フォルダテーブル
CREATE TABLE folders (
  id TEXT PRIMARY KEY,
  name TEXT NOT NULL,
  parent_id TEXT,
  created_at TEXT NOT NULL,
  updated_at TEXT NOT NULL,
  FOREIGN KEY (parent_id) REFERENCES folders (id) ON DELETE CASCADE
);

-- ノートテーブル
CREATE TABLE notes (
  id TEXT PRIMARY KEY,
  title TEXT NOT NULL,
  content TEXT NOT NULL,
  folder_id TEXT,
  created_at TEXT NOT NULL,
  updated_at TEXT NOT NULL,
  FOREIGN KEY (folder_id) REFERENCES folders (id) ON DELETE CASCADE
);

-- タグテーブル
CREATE TABLE tags (
  id TEXT PRIMARY KEY,
  name TEXT NOT NULL UNIQUE
);

-- ノートとタグの関連テーブル
CREATE TABLE note_tags (
  note_id TEXT NOT NULL,
  tag_id TEXT NOT NULL,
  PRIMARY KEY (note_id, tag_id),
  FOREIGN KEY (note_id) REFERENCES notes (id) ON DELETE CASCADE,
  FOREIGN KEY (tag_id) REFERENCES tags (id) ON DELETE CASCADE
);

-- 画像添付テーブル（大きなコンテンツ分離）
CREATE TABLE note_attachments (
          id TEXT PRIMARY KEY,
  note_id TEXT NOT NULL,
  content_type TEXT NOT NULL,
  data BLOB NOT NULL,
          created_at TEXT NOT NULL,
  FOREIGN KEY (note_id) REFERENCES notes (id) ON DELETE CASCADE
);

-- データベースバージョン管理テーブル
CREATE TABLE db_version (
  version INTEGER PRIMARY KEY,
  applied_at TEXT NOT NULL
);
```

### 7.4 パフォーマンス最適化戦略

1. **大規模ノート対策**
   - コンテンツの圧縮保存（大きなノート向け）
   - 添付ファイルの分離保存（note_attachmentsテーブル）
   - 検索インデックス最適化

2. **UI最適化**
   - ノート一覧の仮想化リスト（FlatList最適化）
   - メモ化コンポーネント（React.memo）
   - 遅延読み込みとページネーション

3. **WebView最適化**
   - リソースの事前バンドル
   - DOM操作の最小化
   - メモリリーク監視と防止

### 7.5 主要コンポーネント設計

#### 7.5.1 ノート一覧（app/(tabs)/notes.tsx）
- フォルダ階層ナビゲーション
- ノート/フォルダリスト表示
- 新規作成FAB（フローティングアクションボタン）
- 右上AIアシストボタン

#### 7.5.2 ノートエディタ（app/notes/[id].tsx）
- ヘッダー（戻る、保存、AIアシスト）
- タイトル編集エリア
- WebViewエディタ
- タグ管理エリア
- AIアシストドロワー（画面下から表示）

#### 7.5.3 WebViewエディタ（components/WebViewEditor.tsx）
- TipTap/TOASTエディタブリッジ
- ネイティブ-Web通信
- テキスト選択イベント処理
- アクションバブル表示

#### 7.5.4 AIアシスト（components/note/NoteAIAssist.tsx）
- モード切替（質問/編集/部分）
- チャットインターフェース
- 差分表示
- 編集提案適用機能

#### 7.5.5 タグセレクタ（components/note/TagSelector.tsx）
- タグ一覧表示
- タグ追加/編集/削除
- 選択管理

### 7.6 実装ステップ詳細

1. **データベース基盤実装**
   - SQLiteセットアップ
   - スキーマ作成
   - データアクセスレイヤー実装
   - マイグレーション管理

2. **ノート一覧基本UI**
   - フォルダ/ノート表示
   - ナビゲーション機能
   - 作成/削除機能
   - FAB実装（タブと重ならない配置）

3. **WebViewエディタ基盤**
   - HTML/JSリソース準備
   - WebViewブリッジ設計
   - 基本編集機能実装
   - 自動保存機能

4. **テキスト選択サポート**
   - 選択イベント処理
   - アクションバブル表示ロジック
   - ネイティブ側連携

5. **タグ機能実装**
   - タグCRUD操作
   - ノートとタグの関連付け
   - タグセレクタUI
   - タグオーガナイザー

6. **AIアシスト連携**
   - OpenRouter API連携
   - ノート内容の文脈理解
   - 編集提案生成
   - 差分表示と適用

7. **エラー処理強化**
   - オフライン検出と対応
   - 保存失敗リカバリ
   - APIエラー処理
   - ユーザーへのフィードバック

8. **パフォーマンス最適化**
   - 大量データの効率的処理
   - レンダリング最適化
   - メモリ使用量監視
   - バックグラウンド処理

### 7.7 エラーハンドリング戦略

1. **保存エラー対策**
   - 自動バックアップ（メモリキャッシュ）
   - 変更の一時保存
   - 再試行メカニズム

2. **ネットワークエラー**
   - オフライン状態検出
   - キューイングシステム
   - 同期再開ロジック

3. **APIエラー**
   - クォータ超過通知
   - レート制限対応
   - エラー回復フロー

4. **ユーザー体験**
   - エラー表示の最適化
   - 復旧アクションの提示
   - 進行状況の透明化

### 7.8 テスト計画

1. **単体テスト**
   - データレイヤーテスト
   - UIコンポーネントテスト
   - WebViewブリッジテスト

2. **統合テスト**
   - 主要フロー検証
   - エラーケース検証
   - パフォーマンステスト

3. **ユーザビリティテスト**
   - エディタ操作性
   - AIアシスト有効性
   - オフライン動作確認

### 7.9 オフライン対応

1. **基本原則**
   - すべての基本操作はオフラインで動作
   - AIアシストのみオンライン必須

2. **同期戦略**
   - ローカルファースト設計
   - 変更のキューイング
   - 接続復帰時の処理

3. **UX考慮**
   - オフライン状態表示
   - 機能制限の明示
   - オンライン復帰時の通知

### 7.10 画面レイアウト

#### ノート一覧画面
```
┌────────────────────────────┐
│ [←] ノート        ✨        │ ← 右上AIアシスト
│                            │
│ [📁 フォルダA]              │
│ [📝 ノート1]                │
│ ...                        │
│                            │
│        ＋                  │ ← 右下FAB（タブと重ならない位置）
└────────────────────────────┘
```

#### ノートエディタ画面
```
┌────────────────────────────┐
│ [←] ノート編集    ✨        │ ← 右上AIアシスト
│                            │
│ [タイトル]                  │
│                            │
│ [WYSIWYGエディタ]           │
│  （タップで編集モード）      │
│                            │
│ タグ: #AI #メモ             │
└────────────────────────────┘
```

#### AIアシストドロワー
```
┌────────────────────────────┐
│ AIアシスト  [質問/編集/部分] │ ← モード切替
│                            │
│ [チャットUI]                │
│                            │
│ [プロンプト入力欄]          │
│ [送信]                      │
└────────────────────────────┘
```
