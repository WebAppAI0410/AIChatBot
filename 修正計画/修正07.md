## 7. ノートモジュール（改訂実装計画）

### 7.1 機能概要

- **ノート管理**: フォルダ階層構造でのノート整理、タグ付け。 **（DB/ストアレベルでは実装済み、UIは実装完了）**
- **エディタ**: **TenTap (@10play/tentap-editor)** を使用するWYSIWYGエディタ。React Native向けに最適化されたTipTapベースのエディタで、Markdownサポート、拡張性、カスタマイズ性を重視。Undo/Redo、ハイライト機能などを実装済み。 **（実装完了・更新：2025/01/26）**
    - **マークダウンスタイリング**: GitHub風のマークダウンスタイル適用済み
    - **ダークモード対応**: ライトモード・ダークモード切り替え対応済み
    - **見出し機能**: **H1-H6見出しボタン追加（2025/01/26更新）**
        - **カスタムツールバー**: H1-H6の専用ボタンをツールバーに配置
        - **toggleHeadingコマンド**: TenTapの標準APIを使用した見出し切り替え
        - **アクティブ状態表示**: 現在の見出しレベルを視覚的に表示
        - **ユーザーフレンドリー**: HTMLタグ入力不要でワンクリック見出し設定
    - **基本フォーマット**: Bold、Italic、Underline、リスト、引用等の基本機能
    - **AI校正機能**: **（実装完了・大幅更新：2025/01/26）**
        - **Cursor風AIチャット機能**: **（新機能追加：2025/01/26）**
            - **リアルタイム修正提案**: AIが提案した変更をリアルタイムで検出・表示
            - **メッセージ末尾アクション**: 「変更を提案」ボタンを削除し、メッセージごとに「適用」「取り消し」ボタンを配置
            - **直接コンテンツ操作**: 差分表示を経由せず、AIチャットから直接ノート内容を変更可能
            - **取り消し機能**: 最後の変更を簡単に取り消し可能
            - **処理中表示**: AI処理中のメッセージを明確に表示
        - **画面分割AI校正チャット**: **（実装完了・更新：2025/01/26）**
            - **分割画面レイアウト**: AI校正モード時に画面右側にチャット画面を表示
            - **ヘッダー統合モデル選択**: チャット独自ヘッダーを削除し、ノート編集画面ヘッダーにモデル選択機能を統合
            - **改良版差分表示**: **（Cursor風UI改善：2025/01/26）**
                - **Git風のプレフィックス**: +（追加）、-（削除）、~（修正）の視覚的表示
                - **コードブロック風表示**: 変更内容をコードブロック形式で表示
                - **処理中ステータス**: リアルタイム処理の進行状況を表示
                - **改良されたカラーリング**: ダークモード対応の洗練された色彩
            - **変更提案抽出**: AIチャット応答から具体的な修正提案を自動抽出
            - **一括操作**: すべての変更をまとめて承認・拒否する機能
            - **レスポンシブデザイン**: タブレット・スマホでの分割比率最適化
        - **従来のドロワー形式**: テキスト選択ベースのAI処理（校正・要約・翻訳・改善・拡張）
- **AIアシスト**: ノート全体または部分的な編集支援、質問応答。 **（実装完了：2025/01/26）**
    - **OpenRouter API連携**: プラン別AIモデル使用（GPT-4o-mini/GPT-4o/Claude-3-Sonnet）
    - **リアルタイム処理**: 選択テキストまたはノート全体のAI処理
    - **エラーハンドリング**: APIキー未設定・ネットワークエラー対応
- **フォルダ階層UI**: **（実装完了：2025/01/26）**
    - **階層ナビゲーション**: フォルダ間の移動とパンくずナビゲーション
    - **フォルダ作成・編集・削除**: インライン編集とスワイプ削除
    - **検索機能**: **（実装完了：2025/01/26）**
        - **全階層検索**: フォルダ・ノート名、ノート内容の検索
        - **リアルタイム検索**: 入力と同時にフィルタリング
        - **検索結果プレビュー**: ノート内容の一部表示
- **ストレージ**: SQLiteベースのローカルストレージ（オフライン対応、コンテンツ圧縮機能あり）。 **（実装済み）**

### 7.2 プラン別AIモデルとクォータ

| プラン | AIモデル | 制限 | トークン上限 |
|--------|---------|------|------------|
| Free | openai/gpt-4o-mini | 5回/日 | 1,000トークン/回 |
| Lite | openai/gpt-4o | 20回/日 | 2,000トークン/回 |
| Premium | anthropic/claude-3-sonnet-20240229 | 50回/日 | 4,000トークン/回 |
*（このセクションは現状のコードベースと直接的な依存関係が少ないため、計画通りとします）*

### 7.3 データベース設計（SQLite）

*（`services/sqlite.ts` の実装に基づき、`is_compressed` カラムが `notes` テーブルに存在することを明記）*
```sql
-- フォルダテーブル
CREATE TABLE folders (
  id TEXT PRIMARY KEY,
  name TEXT NOT NULL,
  parent_id TEXT,
  created_at TEXT NOT NULL,
  updated_at TEXT NOT NULL,
  FOREIGN KEY (parent_id) REFERENCES folders (id) ON DELETE CASCADE
);

-- ノートテーブル
CREATE TABLE notes (
  id TEXT PRIMARY KEY,
  title TEXT NOT NULL,
  content TEXT NOT NULL,
  folder_id TEXT,
  created_at TEXT NOT NULL,
  updated_at TEXT NOT NULL,
  is_compressed INTEGER DEFAULT 0, -- コンテンツ圧縮フラグ
  FOREIGN KEY (folder_id) REFERENCES folders (id) ON DELETE CASCADE
);

-- タグテーブル
CREATE TABLE tags (
  id TEXT PRIMARY KEY,
  name TEXT NOT NULL UNIQUE
);

-- ノートとタグの関連テーブル
CREATE TABLE note_tags (
  note_id TEXT NOT NULL,
  tag_id TEXT NOT NULL,
  PRIMARY KEY (note_id, tag_id),
  FOREIGN KEY (note_id) REFERENCES notes (id) ON DELETE CASCADE,
  FOREIGN KEY (tag_id) REFERENCES tags (id) ON DELETE CASCADE
);

-- 画像添付テーブル（大きなコンテンツ分離）
CREATE TABLE note_attachments (
          id TEXT PRIMARY KEY,
  note_id TEXT NOT NULL,
  content_type TEXT NOT NULL,
  data BLOB NOT NULL,
          created_at TEXT NOT NULL,
  FOREIGN KEY (note_id) REFERENCES notes (id) ON DELETE CASCADE
);

-- データベースバージョン管理テーブル
CREATE TABLE db_version (
  version INTEGER PRIMARY KEY,
  applied_at TEXT NOT NULL
);
```

### 7.4 パフォーマンス最適化戦略

1.  **大規模ノート対策**
    *   コンテンツの圧縮保存（実装済み、`notes.is_compressed` で管理）
    *   添付ファイルの分離保存（`note_attachments` テーブル、実装済み）
    *   検索インデックス最適化（一部実装済み）
2.  **UI最適化**
    *   ノート一覧の仮想化リスト（`FlatList` で実装済み）
    *   メモ化コンポーネント（`React.memo`）（適宜使用）
    *   遅延読み込みとページネーション（必要に応じて検討）
3.  **TenTapエディタ最適化**
    *   TenTapの最適化機能を活用（React Native向けに最適化済み）。
    *   ネイティブパフォーマンスの活用（ブリッジ通信なし）。
    *   リッチテキスト編集のレンダリング最適化。
    *   メモリリーク監視と防止（継続的な課題）。

### 7.5 主要コンポーネント設計

#### 7.5.1 ノート一覧（`app/(tabs)/notes.tsx`）
-   フォルダ階層ナビゲーション（実装済み）
-   ノート/フォルダリスト表示（実装済み）
-   ヘッダー右側に「新規ノート作成」ボタンおよび「リスト/グリッド表示切替」ボタン配置（新規ノート作成は機能、表示切替ボタンはあるがグリッド表示ロジックは未実装）。

#### 7.5.2 ノートエディタ（`app/notes/[id].tsx`）
-   ノート編集画面のメインコンテナ。**（実装完了・更新：2025/01/26）**
-   ヘッダーに戻るボタン、ノートタイトル表示/編集、Undo/Redo、AI校正チャットボタン、AIモデル選択機能。
-   `components/note/TenTapEditor.tsx` を使用したエディタ表示。
-   **AI校正分割画面**: AI校正モード時に画面を左右に分割し、右側にチャット画面を表示
-   **Cursor風差分表示**: `components/note/DiffViewer.tsx` を使用した変更提案の差分表示
-   タグ管理エリア（`components/note/TagSelector.tsx` を使用予定）。
-   自動保存機能は `store/noteStore.ts` と連携して実装済み。

#### 7.5.3 TenTapエディタ（`components/note/TenTapEditor.tsx`）
-   **TenTap (@10play/tentap-editor)** ライブラリを使用。**（実装完了・簡素化：2025/01/26）**
-   基本的なリッチテキスト編集機能（Markdownサポート）、Undo/Redo、ハイライト機能、ダークモード対応などを実装完了。
-   React Nativeのネイティブコンポーネントとして動作（WebViewなし）。
-   **ドラッグ&ドロップ機能削除**: 複雑性を排除し、よりシンプルなエディタに
-   テキスト選択イベントの処理とコールバック実装完了。

#### 7.5.4 AI校正チャット（`components/note/AIAssistChat.tsx`）
-   **実装完了・更新（2025/01/26）**
-   **ヘッダー削除**: 「AI校正アシスタント」ヘッダーを削除し、より多くの画面領域を確保
-   **モデル選択機能削除**: モデル選択機能をノート編集画面ヘッダーに移動
-   ノート内容をコンテキストとしたAI対話機能
-   **変更提案機能**: 「変更を提案」ボタンで差分表示機能と連携
-   レスポンシブデザイン対応（タブレット・スマホ最適化）

#### 7.5.5 差分表示ビューア（`components/note/DiffViewer.tsx`）
-   **新規実装（2025/01/26）**
-   **Cursor風UI**: 変更提案を追加・削除・修正で色分け表示
-   **個別操作**: 各変更に対してaccept/rejectボタン
-   **一括操作**: すべての変更をまとめて承認・拒否
-   **ステータス表示**: 適用済み・拒否済みの変更を明確に表示
-   **モバイル最適化**: タッチ操作に最適化されたUI

#### 7.5.6 タグセレクタ（`components/note/TagSelector.tsx`）
-   現状未確認。DB/ストアレベルではタグ管理機能は実装済み。
-   ノート編集画面で使用するタグ一覧表示、タグ追加/編集/削除、ノートへのタグ付けUIを実装予定。

### 7.6 実装ステップ詳細

1.  **データベース基盤実装**: **完了**
    *   SQLiteセットアップ (`services/sqlite.ts`)
    *   スキーマ作成とマイグレーション管理 (`services/sqlite.ts`)
    *   データアクセスレイヤーと状態管理 (`store/noteStore.ts`)
    *   コンテンツ圧縮機能の実装

2.  **ノート一覧基本UI (`app/(tabs)/notes.tsx`)**: **完了**
    *   フォルダ/ノート表示、ナビゲーション機能（完了）
    *   ヘッダーに新規ノート作成ボタン、表示切替ボタン設置（完了、ただしグリッド表示ロジックは未実装）
    *   ノート/フォルダの削除・編集UIの追加（完了）
    *   検索機能の実装（完了）

3.  **TenTapエディタ基盤 (`components/note/TenTapEditor.tsx`)**: **完了（2025/01/26）**
    *   TenTapエディタのインストールと基本設定。
    *   Markdownコンテンツの表示・編集機能の実装。
    *   基本的な書式設定（見出し、リスト、太字、斜体など）のツールバー連携。
    *   Undo/Redo機能の実装。
    *   ノート編集画面 (`app/notes/[id].tsx`) への統合と自動保存機能連携。
    *   ダークモード対応とGitHub風マークダウンスタイリング
    *   **ドラッグ&ドロップ機能の削除**（簡素化）

4.  **AI校正機能実装**: **完了・更新（2025/01/26）**
    *   **画面分割チャット機能**: AI校正モード時の分割画面レイアウト
    *   **AIAssistChat コンポーネント**: 専用チャットインターフェース（ヘッダー削除）
    *   **モデル選択機能統合**: ノート編集画面ヘッダーにAIモデル選択機能を移動
    *   **Cursor風差分表示**: DiffViewerコンポーネントによる変更提案の可視化
    *   **変更提案抽出**: AIチャット応答から具体的な修正提案を自動抽出
    *   **一括操作機能**: すべての変更をまとめて承認・拒否
    *   **従来のドロワー機能**: テキスト選択ベースのAI処理

5.  **タグ機能実装**: **DB/ストアは完了、UIは未実装**
    *   タグCRUD操作とノートとタグの関連付け (`services/sqlite.ts`, `store/noteStore.ts`)（完了）
    *   タグセレクタUI (`components/note/TagSelector.tsx`) の実装とノート編集画面への統合（未実装）

6.  **エラー処理強化**: **継続的改善**
    *   基本的なエラーハンドリングはストア等に実装済み。
    *   オフライン検出と対応、保存失敗リカバリ、APIエラー処理の強化。

7.  **パフォーマンス最適化**: **継続的改善**
    *   DBには圧縮機能とインデックス、一覧には`FlatList`を使用済み。
    *   大量データの効率的処理、レンダリング最適化、メモリ使用量監視を適宜実施。

### 7.7 エラーハンドリング戦略
*（一般的な戦略であり、現状の実装と大きく矛盾しないため、微修正に留めます）*
1.  **保存エラー対策**
    *   自動バックアップ（メモリキャッシュ検討）
    *   変更の一時保存（`store/noteStore.ts` での状態管理である程度カバー）
    *   再試行メカニズム（必要に応じて検討）
2.  **ネットワークエラー** (主にAIアシスト機能)
    *   オフライン状態検出
    *   キューイングシステム（AI関連操作で検討）
    *   同期再開ロジック（AI関連操作で検討）
3.  **APIエラー** (主にAIアシスト機能)
    *   クォータ超過通知
    *   レート制限対応
    *   エラー回復フロー
4.  **ユーザー体験**
    *   エラー表示の最適化
    *   復旧アクションの提示
    *   進行状況の透明化

### 7.8 テスト計画
*（一般的な計画であり、現状の実装と大きく矛盾しないため、微修正に留めます）*
1.  **単体テスト**
    *   データレイヤーテスト (`services/sqlite.ts`, `store/noteStore.ts`)
    *   UIコンポーネントテスト (各ノート関連コンポーネント)
    *   **TenTapテスト (TenTap連携、段落操作、イベント通知)**
2.  **統合テスト**
    *   ノート作成・編集・削除・タグ付け・フォルダ移動の主要フロー検証
    *   **AI校正機能テスト（分割画面、チャット、差分表示、変更適用）**
    *   エラーケース検証
    *   パフォーマンステスト（特に大量ノート時）
3.  **ユーザビリティテスト**
    *   エディタ操作性
    *   **AI校正機能の使いやすさ（分割画面、差分操作、変更適用体験）**
    *   オフライン動作確認（DB操作はオフライン対応済み）

### 7.9 オフライン対応
1.  **基本原則**
    *   すべての基本ノート操作（CRUD、フォルダ操作、タグ付け）はオフラインで動作（実装済み）。
    *   AIアシストのみオンライン必須（計画通り）。
2.  **同期戦略** (主にAI機能や将来的なクラウド同期を想定)
    *   ローカルファースト設計（実装済み）。
    *   変更のキューイング（AI関連操作で検討）。
    *   接続復帰時の処理（AI関連操作で検討）。
3.  **UX考慮**
    *   オフライン状態表示（汎用的なネットワーク状態表示に依存）。
    *   機能制限の明示（AIアシスト利用時など）。
    *   オンライン復帰時の通知（AIアシスト利用時など）。

### 7.10 画面レイアウト

#### ノート一覧画面 (`app/(tabs)/notes.tsx`)
```
┌────────────────────────────┐
│ [←] ノート一覧/フォルダ名 [📝|🏁]│ ← ヘッダー右: 新規ノート, 表示切替
│                            │
│ [📁 フォルダA]              │
│ [📝 ノート1]                │
│ ...                        │
│                            │
└────────────────────────────┘
```

#### ノートエディタ画面 (`app/notes/[id].tsx`) - 通常モード
```
┌────────────────────────────┐
│ [←] ノートタイトル [↶|↷|💬] │ ← ヘッダー右: Undo, Redo, AIチャット
│                            │
│ [TenTapエディタ]            │
│                            │
│ [タグエリア (TagSelector)]   │
│                            │
│ テキストエディタ（WYSIWYG）  │
│                            │
└────────────────────────────┘
```

#### ノートエディタ画面 (`app/notes/[id].tsx`) - AI校正チャットモード **（実装完了・更新）**
```
┌──────────────────────────────────────────────────────────────────────┐
│ [←] ノートタイトル [↶|↷|GPT-4o ▼|💬] ← ヘッダー右: Undo, Redo, Model, Chat │
├─────────────────────────┬────────────────────────────────────────────┤
│ [TenTapエディタ]         │ [システムメッセージ]                        │
│                         │                                            │
│ テキストエディタ（WYSIWYG）│ [ユーザー] ここを修正して                   │
│                         │                                            │
│                         │ [AI] 修正提案です [変更を提案]              │
│                         │                                            │
│ ┌───────────────────────┐ │                                            │
│ │ 変更提案 (2件)    [✓全][×全]│ │                                            │
│ │ ┌─────────────────────┐ │ │                                            │
│ │ │修正 [×][✓]         │ │ ├────────────────────────────────────────────┤
│ │ │元: "こんにちわ"     │ │ │ [入力欄...] [送信]                          │
│ │ │後: "こんにちは"     │ │ │                                            │
│ │ └─────────────────────┘ │ │                                            │
│ └───────────────────────┘ │                                            │
└─────────────────────────┴────────────────────────────────────────────┘
```
